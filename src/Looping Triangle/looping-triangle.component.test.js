const loopingTriangle = require('./looping-triangle.component');
const {describe, it, expect, test} = require("@jest/globals");

describe('', () => {
    it('should', () =>{
        expect(loopingTriangle(2)).toBe(2);
    });
});